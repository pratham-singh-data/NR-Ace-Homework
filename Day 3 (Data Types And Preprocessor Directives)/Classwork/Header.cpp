#include "Header Practice/myFile.txt"

// Quotation is for out own files, we mention path there. It tries to find in current diectory 
// then in compiler files, if files isn't found then error is returned.
// Angle brackets are for system files, the compilers files are scanned here and nothing else.

int main(){
    cout << Pratham::age;
    return 0;
}